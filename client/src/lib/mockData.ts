import type { ContractWithVendor, Vendor, DashboardStats } from "@shared/schema";

// This file is intentionally left mostly empty as we're using real data from the backend
// Only keeping type exports for consistency

export type { ContractWithVendor, Vendor, DashboardStats };
